{% extends "template.twig" %}

{% block title %}Scenario - List{% endblock %}

{% block head %}
    {{ parent() }}
{% endblock %}

{% block header %}
    {{ parent() }}
{% endblock %}

{% block content %}
    <nav>
        <a href="{{ path('scenario_create') }}" class="btn btn-default">New</a>
    </nav>

    {% if elements is not empty %}
    <ul class="list-group">
        {% for scenario in elements %}
        <li class="list-group-item">
            {{ scenario.name }}
            <span class="tools pull-right">
                <a href="{{ path('scenario_edit', { scenarioId : scenario.id }) }}">
                    <span class="glyphicon glyphicon-pencil" title="edit"></span>
                </a>
            </span>
        </li>
        {% endfor %}
    </ul>

    <nav aria-label="Navigation">
        <ul class="pagination">
            <li {% if pagination.page == 1 %}class="disabled"{% endif %}>
                <a href="{{ path('scenario_list') }}?page={{ pagination.page-1 }}" aria-label="Previous">
                <span>
                    <span aria-hidden="true">&laquo;</span>
                </span>
                </a>
            </li>

            {% for index in range(1, pagination.totalPages) %}
                <li {% if index == pagination.page %}class="active"{% endif %}>
                    <a href="{{ path('scenario_list') }}?page={{ index }}">{{ index }}</a>
                </li>
            {% endfor %}
            <li {% if pagination.page == pagination.totalPages %}class="disabled"{% endif %}>
                <a href="{{ path('scenario_list') }}?page={{ pagination.page+1 }}" aria-label="Next">
                <span>
                    <span aria-hidden="true">&raquo;</span>
                </span>
                </a>
            </li>
        </ul>
    </nav>
    {% else %}
    <p>No scenario here.</p>
    {% endif %}
{% endblock %}

{% block scripts %}
    {{ parent() }}
{% endblock %}
